# doumei-app

### 模型迁移
`php artisan migrate`


### 多应用后台运行
`php artisan agent:seed `

`代理后台 http://localhost/agent-admin`

### Test 
`php artisan test --filter GetMsgCodeTest`
### laravel 清除缓存

`php artisan view:clear` 

`php artisan cache:clear`

`php artisan config:clear` 

`php artisan route:clear`
"# jiuzhouApi" 
